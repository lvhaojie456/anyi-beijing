# 2026-09-17 main 7f8938f 后端与制作端部署记录

## 范围

- 代码版本：`7f8938f448a8c94cb2b2e924a9ff7bd53030ec93`（`main`），包含 PR [#4](https://github.com/lvhaojie456/anyi-beijing/pull/4)、[#5](https://github.com/lvhaojie456/anyi-beijing/pull/5)、[#6](https://github.com/lvhaojie456/anyi-beijing/pull/6)。上一版线上代码为 PR #3 分支的 `e1c8e3a`（见 `2026-09-17-live2d.md`）。
- 后端：`/opt/anyiapp2/backend` 的源码与 `dist-node` 切换到 `7f8938f`。运行时变化只有 PR #5 的死代码清理与迁移运行器重构；`.env`、`node_modules`、Nginx、数据库均未改动。
- Mac 制作端：新增 `~/Library/Application Support/AnyiLive2D/releases/7f8938f`，launchd `cn.anyibj.live2d-worker` 已切换到该目录（PR #6 的嘴部阈值回退与 Astra 流中断重试生效）。
- Android：无变化，线上仍为 1.0.18（versionCode 20）。
- 数据库：无新迁移；`_node_migrations` 部署前后均为 13 行，最新 `0013_live2d_generation.sql`；`migrations-mysql` 目录与 `main` 逐字节一致。

## 后端部署步骤（23:22–23:29 CST）

1. 部署前运行 `anyi-mysql-backup.service`，产物 `/home/ubuntu/anyi-db-backups/anyi-mysql-anyi_memorial-20260917-232532.sql.gz`（30 张表，含 `live2d_jobs`）与 `anyi-uploads-20260917-232532.tar.gz`。
2. 在服务器用同一 `node_modules/.bin/tsc`（5.9.3，Node 20.20.2）先重建 `e1c8e3a`，`dist-node` 的 JS 哈希 `e404f79ea1e3` 与线上完全一致，证明构建方式可复现；再构建 `7f8938f`，JS 哈希 `df3f9db09af7`，与本机 `npm test` 构建一致。
3. 暂存目录 `/opt/anyi-releases/main-20260917-7f8938f`；回滚副本 `/opt/anyi-releases/before-main-20260917-7f8938f`（含旧 `dist-node`、源码、`RELEASE.json`、`.env` 副本，权限 700）；旧编译目录另存为 `/opt/anyiapp2/backend/dist-node.pre-main-20260917-7f8938f`。
4. 切换 `dist-node`、`src`、`server`、`tests`、`scripts`、`examples` 与 `package.json` 等文件，删除已在仓库移除的 `.dev.vars.example`，更新 `RELEASE.json`。
5. `systemctl restart anyi-memorial-api`，停机约 1 秒。

## 验证

- 本机：后端 `npm test` 50 项通过，制作端在新 release 目录用生产 venv 跑 `unittest discover` 21 项通过。
- 线上：`anyi-memorial-api` / `nginx` / `mysql` 均 active；`http://127.0.0.1:8787/health` 与 `https://api.anyibj.cn/health` 返回 `ok`；启动日志正常连接 MySQL，重启后 3 分钟内 journal 无错误；Nginx 重启后请求全部 200；`/admin/asset-delete-queue`、`/ai/live2d/config`、`/ai/companions`、`/memorials` 无凭据访问返回 401，无 500。
- 制作端：切换后进程由 launchd 拉起（`caffeinate -i` 托管），`/internal/live2d/jobs/claim` 每 10 秒返回 200，`worker.err` 无新增；切换时线上没有 queued / running 任务。
- 磁盘：服务器 `/` 使用 67%。

## 回滚

后端：

```bash
cd /opt/anyiapp2/backend
mv dist-node dist-node.main-7f8938f && cp -a dist-node.pre-main-20260917-7f8938f dist-node
sudo systemctl restart anyi-memorial-api && curl -fsS http://127.0.0.1:8787/health
```

源码与 `RELEASE.json` 可从 `/opt/anyi-releases/before-main-20260917-7f8938f/backend` 复制回来；`.env` 未改动，无需恢复；无迁移需要回退。

制作端：把 `~/Library/LaunchAgents/cn.anyibj.live2d-worker.plist` 恢复为同目录下的 `.bak-e1c8e3a` 副本，然后 `launchctl bootout gui/$UID/cn.anyibj.live2d-worker && launchctl bootstrap gui/$UID ~/Library/LaunchAgents/cn.anyibj.live2d-worker.plist`。

## 已知问题

线上任务 `5462ebc4`（白底写实老爷爷）即使在新制作端重试，仍会在校验阶段以脚底位移 0.32 px（阈值 0.25）失败：4090 See-through 把整个白色背景并入了 `topwear` 图层。需要换非白底图片重新生成，或另行修改拆层后处理；详见 CHANGELOG 运维小节。
